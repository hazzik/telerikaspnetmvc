/*
namespace Telerik.Web.Mvc.UI.UnitTest.Grid
{
    using Mvc.UI;

    using Xunit;

    public class GridPagerBuilderTests
    {
        private readonly GridPagerBase _pagerBase;
        private readonly GridPagerBuilder builder;
        
        public GridPagerBuilderTests()
        {
            _pagerBase = new GridPagerBase();
            builder = new GridPagerBuilder(_pagerBase);
        }

        [Fact]
        public void Position_sets_pager_position()
        {
            builder.Position(GridPagerPosition.Top);

            Assert.Equal(GridPagerPosition.Top, _pagerBase.Position);
        }

        [Fact]
        public void PageSize_sets_page_size()
        {
            builder.PageSize(1);

            Assert.Equal(1, _pagerBase.PageSize);
        }
    }
}
*/